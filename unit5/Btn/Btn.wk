div {
    java = ["div", "h1"];
    # async {
        true
    } class = [""]
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    # {
        let m = "d";
        $self.java.filter((i) => i.startswith(m))
    } {
        $self.
    }
}